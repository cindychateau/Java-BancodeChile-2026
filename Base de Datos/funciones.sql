-- FUNCIONES: COUNT(), SUM(), AVG(), MIN(), MAX()
SELECT COUNT(id) FROM trucos; -- Contar

SELECT SUM(edad) FROM mascotas; -- Sumar

SELECT AVG(edad) FROM mascotas; -- Promedio

SELECT MIN(fecha_nacimiento) FROM duenos; -- Valor minimo

SELECT MAX(fecha_nacimiento) FROM duenos; -- Valor maximo